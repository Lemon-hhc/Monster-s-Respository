#include<stdio.h>
int main()
{
  int ch =0;
  while ((ch=getchar()) != EOF) // EOF = end of file
  {
   if (ch<'0' || ch>'9')
   continue;
   putchar(ch);
    
  }
  return 0;
}
  //if you input number it will output; other's input will not to be output.
